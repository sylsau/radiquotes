* use 'declare arg1=... arg2=...' at top of func instead of spreading assignements on several lines
* use libparse everywhere
* quotes format
	* allow newlines (replaced by \n):
		* detect if several lines before "^---"
		* Make $ -> \n
		* e.g.:
			this quote
			is long
			--- me

		  will turn into:
			this quote\nis long@me
* refaire situation.sh !!!!
	* parameter for line height in imagemagick?
		* seems not possible
	* option to enable/disable white guillemets
